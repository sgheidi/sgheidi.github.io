A generalized minimax and alpha-beta pruning algorithm for perfect, 2-player games

I struggled with the concept of minimax for quite a while before running into *this* repo, which
contained the code for exactly what I was looking for, for my chess AI.

The main difficulty for coding a minimax ... is the undo function...

Here's the code for a ...

...
Player1.move(piece, pos) // pos[0] -> x. pos[1] -> y
Board.undo()



...
Cards with blog posts (snippets) in main content
Links to github, linkedin beside light/dark mode

Commenting, upvoting on blog posts. Connect to Firebase or MongoDB.
